{% extends './layout.twig' %}

{% block content %}
    {% include './components/avatar/avatar_states.twig' %}

    {# undefined, хотя шаблон с переменными импортится, но скоуп переменных не распростроняется на текущий файл #}
    {{ dump(AvatarSize) }}

    {% include './components/avatar/avatar.twig' with {
        text: 'Pam'
    } %}

    {# Желаемый результат. Состояния компонента доступны для использоваиня
    {% include './components/avatar/avatar.twig' with {
        text: 'lalal'
        size: AvatarSize.raffle
        style: AvatarStyle.desmon
        isRounded: AvatarIsRounded.value0
    } %}#}
{% endblock %}
